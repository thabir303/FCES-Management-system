{
  "permissions": {
    "allow": [
      "Bash(.venv/bin/pytest research/tests:*)",
      "Bash(make test:*)",
      "Bash(make paper:*)",
      "Bash(make tables:*)",
      "Bash(make smoke:*)",
      "Bash(git status:*)",
      "Bash(git log:*)",
      "Bash(git diff:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys; d=json.load(sys.stdin); p=d.get('tool_input',{}).get('file_path',''); sys.exit(0) if 'results/tables/' not in p else (sys.stderr.write('results/tables/ is generated by make_tables.py and must never be hand-edited. Change the builder, then run: make tables\\n'), sys.exit(2))\""
          }
        ]
      }
    ]
  }
}
