.venv/
.pgdata/
.cache/
__pycache__/
*.pyc
*.egg-info/
.env
.DS_Store
FCES-code 
FCES-code.zip
# Corpora are distributed as identifiers + reconstruction scripts, never redistributed.
data/raw/
data/interim/
data/processed/

storage/
node_modules/
.next/

# Generated. `make tables` rebuilds these from results/runs/.
results/tables/*
!results/tables/.gitkeep

# Generated. `make figures` rebuilds these from results/runs/.
results/figures/*
!results/figures/.gitkeep

# Run artefacts are committed; the LLM ledger is too (it is the cost evidence).
PROJECT_PLAN.amendments.diff
*.aux
*.log
*.out
*.pdf

# Mechanically regenerated from main.tex + results/tables/* for Overleaf upload; never
# hand-edited, so not a source file worth tracking.
main_overleaf.tex

# Agent-local, never committed
.claude/settings.local.json
CLAUDE.local.md
